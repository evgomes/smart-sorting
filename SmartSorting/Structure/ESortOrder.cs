namespace SmartSorting.Structure
{
    /// <summary>
    /// Used to determine if a sort algorithm should sort in ascending or descending direciton.
    /// </summary>
    public enum ESortOrder : byte
    {
        Ascending = 0,
        Descending = 1
    }
}